# AGENTS.md

## Git Workflow Rules
- 作業開始前に必ず作業用ブランチを作成する（例: `feature/*`, `fix/*`, `chore/*`, `docs/*`, `refactor/*`）。
- 変更は必ず作業用ブランチでコミットする。
- `push` 後に Pull Request を作成する。
- `main` への反映は Pull Request 経由のみとする。
- `main` での直接コミットは行わない。
